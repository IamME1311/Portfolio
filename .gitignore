# Node.js (used for PDF generation script only, not part of the site)
node_modules/
package-lock.json
package.json

# Local dev files
.DS_Store
Thumbs.db

# assets to ignore
assets/resume.html

*.md
*mcp

# Project #3 app — separate project, will be moved to its own repo later
llm-gateway/